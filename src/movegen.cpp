#include "movegen.hpp"

std::vector<int> MoveGen::genPawn(int location, bool isBlack){
    std::vector<int> generated; 
    if(!isBlack){
        bool isFirst = (location > 7 && location < 17); 
        
    }

  }